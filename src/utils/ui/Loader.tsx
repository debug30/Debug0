const Loader = () => {
  return (
    <span className="absolute top-0 left-0 flex items-center justify-center bg-[#ffffff75] w-full h-full">
      <span className="loader"></span>;
    </span>
  );
};

export default Loader;
